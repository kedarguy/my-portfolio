{
  "name": "guy-kedar-portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "npm-run-all --parallel serve styles",
    "serve": "lite-server",
    "styles": "node-sass -w scss/style.scss css/main.css",
    "build": "rm -rf build && copyfiles assets/* index.html assets/fonts/**/* dist/* build"
  },
  "devDependencies": {
    "copyfiles": "^1.0.0",
    "lite-server": "^2.3.0",
    "node-sass": "^3.13.0",
    "npm-run-all": "^3.1.1"
  },
  "dependencies": {
    "normalize.css": "^5.0.0"
  }
}
